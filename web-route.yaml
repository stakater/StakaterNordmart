apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/instance: NAMESPACE-web
    app.kubernetes.io/managed-by: Tiller
    app.kubernetes.io/name: web
    expose: "true"
  name: web
spec:
  host: web-NAMESPACE.DOMAIN
  port:
    targetPort: tcp
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: web
    weight: 100
  wildcardPolicy: None
